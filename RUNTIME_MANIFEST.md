# CITY Runtime Manifest

- Selected successor: `PHASE_5_SELECTION.json`; Phase 5 specification frozen.
- Frozen exact draft-3 spec: `Live Cross-Domain Evidence Round-Trip Proof - Draft.md`.
- Freeze: `PHASE_5_FREEZE.json`; exact review preserved under `References/Phase5SpecificationReview/draft3/`.
- Document check: `python3 -B proof_kernel/validate_live_cross_domain_evidence_round_trip_spec.py --self-test --json`.
- Successor MCDP session: `mcdp-city-live-evidence-spec`; no P16/Phoenix handoff yet.
- Native entry: `./start.sh`.
- Interface: `.controltower/interface.json`.
- Adapter: `.controltower/native-adapter.yaml`, generated by ControlTower.
- Truth: canonical Python records; current continuation `0.7.0-draft.83` and sealed Phase-4 evidence.
- Integrity baseline: `tools/controltower/baseline.json`, pinned to imported commit `8b176e9b6db3eaf06b8e8cca391960f4a1532149`.
- Mount validation: `./start.sh --validate --json`.
- Full stored-release validation: `./start.sh verify-release --json`.
- Exact-HEAD receipt: `.controltower/receipts/native-city-verify-release.json`.
- Contract cases: `python3 -B tests/controltower/contract_cases.py --case <case> --json`.
- Profile: `.controltower/profiles/city_agent.agent-profile.json`.
- MCDP: Projects-root `protocols/MCDP_v3_AGENT_PROTOCOL.json`. Mount a CITY session through ControlTower before lane-bound work.
- Unreal project: `CityMaterializationProof/CityMaterializationProof.uproject`. Live execution from CITY is unverified.

Runtime receipts are ignored. Original proof source, manifests, and evidence remain tracked and unchanged. Query completion does not imply game health or trusted CI.
