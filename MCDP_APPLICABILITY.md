# CITY MCDP applicability

The operator approved the scoped continuation repair after the MCDP P4 attack found a sequencing conflict. The existing live acceptance test required implementation before CITY's continuation rules permitted it. The session returned to P2. The game specification remains frozen.

The governing forward record is [CITY_MCDP_APPLICABILITY.json](../protocols/CITY_MCDP_APPLICABILITY.json). ControlTower owns and hash-pins that record. It binds candidate `712fff25e3e256a77b733f7ca108aa9d7ad00d1e`, its frozen specification/contract bytes and the existing forward freeze. The operator instruction was `Approved`, for [the scoped proposal](../Synthesis/CITYPhase5Adversarial/PROPOSED_APPLICABILITY_REPAIR.md).

MCDP verifies the intermediate implementation contract. Phoenix must implement and prove the unchanged live game acceptance. This distinction changes continuation governance. It does not change the frozen game requirements or any historical seal.

P5 must exercise the smallest executable contract model. P9 must execute a vertical contract verification path. Neither claims live Unreal acceptance. The root runtime recalculates the pinned canonical model and requires exact phase projections before running the two independent completion validators. P10 configures only the frozen witness/failure set. P16 can emit only an actual `implementation_contract_sealed` handoff targeting CITY. Game implementation still requires the real emitted Phoenix route and a clean implementation contract.

The revised P2 model has one intermediate purpose: make the frozen implementation contract executable and independently checkable. Inputs are the exact candidate, schemas, canonical records, failure schedules and approval record. Outputs are verified phase contracts and a bounded P16 handoff. State is the preserved MCDP session and its hash-bound evidence. Transformations construct, attack, repair and verify that contract. Python retains canonical game truth. The ultimate acceptance remains the same two original live domains receiving and displaying one settled result across all frozen witnesses and failure runs.

The forward constitution keeps the original game laws, scope, ownership and non-claims. Intermediate acceptance has its own identity and evidence class. It cannot be substituted for live acceptance. Approval cannot be transferred by rehashing a local record. Phase completion requires the registered runtime checks and actual phase evidence. The historical P2/P3 records remain history; this amendment does not claim those reconstructed phases have completed.

From the Projects root:

```sh
./ControlTower mcdp applicability validate --session mcdp-city-live-evidence-spec --json
./ControlTower mcdp next mcdp-city-live-evidence-spec --json
```

Binding and validation write development receipts under `Synthesis/AgentProtocols/mcdp-city-live-evidence-spec/`. Read the current state there. Installation alone grants no completed phase, live run, Phoenix mount, game implementation or Phase-5 seal.
