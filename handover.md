# THE_CITY Session Handover

**Generated:** 2026-08-29T21:57:30Z

**Repository:** `/Users/boandersson/Desktop/Games/THE_CITY`

**Purpose:** Semantic and executable handover for the next repository session

**Authority:** Committed informational successor only; this file is not a proof contract, seal, capacity record, release member, or implementation authorization

## Phase-4 specification freeze — 2026-08-29

This is the current operational record. It supersedes the Phase-4 candidate,
review-status, authority, current-working-unit, and next-gate statements in
every later historical section of this handover.

```yaml
freeze_commit: 239a105463ef1d928a06b010918f197bba330fe3
freeze_tree: 8944f4881deb2d55912fb90b72926e64179c2be8
freeze_parent: 0f0d6ed6365fa4b07f15b5d9c1d021992afc7d14
freeze_subject: Freeze cross-domain occupancy materialization proof
accepted_candidate_commit: fd2d62f404936eb961d7d1571204f1a352439bd7
accepted_candidate_tree: b8b7bd6259d72c685ac5dee3c1488b19025b0fb3
independent_final_freeze_review: ACCEPT_FOR_FREEZE
frozen_spec_sha256: 47889ac299cf2cfbea143a6a529b1253826e74ae9ddc83f0b25903117ff9406d
review_validator_sha256: 5b58e044f23bef743c3cf5730406aaed0bd21c5f1e31ce0c280ceac55c4961c8
governing_continuation: 0.7.0-draft.81
working_unit: Cross-Domain Canonical Occupancy Materialization Proof v0.1.0 bounded implementation
phase: 4
frozen_proof_harness_identity: CrossDomainCanonicalOccupancyMaterializationProof.v1
simulation_identity: 0.7.0-draft.80
specification_status: frozen
freeze_status: frozen
implementation_authority: bounded_phase_4_proof_only
unreal_source_change_authority: exact_frozen_phase_4_paths_only
evidence_authority: exact_frozen_phase_4_evidence_only
release_authority: exact_frozen_phase_4_release_only
evidence_status: unsealed
artifact_members: 82
manifest_members_excluding_manifest: 172
review_validator: 20/20_checks_and_32/32_mutations_rejected
predecessor_regressions: 215/215
relative_links: 110/110
capacity_record: THE_CITY Development Capacity and Progress Note v0.1.11
capacity_advancement: none
phase_5: closed
implementation_performed_by_freeze: false
push_performed: false
next_gate: exact_bounded_phase_4_implementation_and_evidence_acquisition
```

Independent final review accepted the exact candidate commit and tree above.
The forward freeze records that verdict without rewriting the reviewed
candidate or any sealed predecessor. It grants only the specification's exact
four new Python paths, ten new Unreal paths, bounded
`CityProofGameMode.cpp` branch, evidence path, four governing paths,
82-member artifact directory, and self-excluding 172-member release boundary.
The specification itself is immutable during implementation; the review
validator remains non-release QA; `handover.md` remains an informational
excluded record.

The freeze transition changed only the five governing/specification documents
and the non-release validator. It created no Phase-4 runtime source, Unreal
source, evidence, artifact, or manifest, performed no release construction,
advanced no capacity, opened no Phase 5 work, and performed no push.

## Phase-4 corrective final-freeze candidate — 2026-08-29

This is the current operational record. It supersedes the Phase-4 candidate,
review-status, current-working-unit, and next-gate statements in every later
historical section of this handover.

```yaml
corrective_candidate_commit: fd2d62f404936eb961d7d1571204f1a352439bd7
corrective_candidate_tree: b8b7bd6259d72c685ac5dee3c1488b19025b0fb3
corrective_candidate_subject: Close final Phase 4 freeze-review findings
corrective_spec_sha256: 0914e980ac963da0046153fb8ab0385c0fb20eef631686ed2f31f539e6285be2
review_validator_sha256: 991d8d72ee46973c1936cacca8c4addb9bf5ddfd8eae41a6a55816f520c024ca
governing_continuation: 0.7.0-draft.80
working_unit: Cross-Domain Canonical Occupancy Materialization Proof v0.1.0-draft.1
phase: 4
candidate_proof_harness_identity: CrossDomainCanonicalOccupancyMaterializationProof.v1
candidate_simulation_identity: 0.7.0-draft.80
specification_status: final_freeze_review_candidate
freeze_status: not_frozen
initial_independent_verdict: STOP_WITH_FINDINGS
first_corrective_candidate_commit: 299aa865c5757fb63aeef27a9c4eb48554dcea6f
first_corrective_candidate_tree: 666bf7c70f04a4b1294c08aa5e003b97ff5fd836
first_final_freeze_review_verdict: STOP_WITH_FINDINGS
precommit_residual_audit: ACCEPT_FOR_CORRECTIVE_COMMIT
precommit_residual_targeted_mutations: 8/8_rejected
final_freeze_review_status: pending_exact_commit_review
review_validator: 20/20_checks_and_32/32_mutations_rejected
predecessor_regressions: 215/215
implementation_authority: none
unreal_source_change_authority: none
evidence_authority: none
release_authority: none
capacity_record: THE_CITY Development Capacity and Progress Note v0.1.11
capacity_advancement: none
phase_5: closed
push_performed: false
next_gate: independent_final_freeze_review_of_exact_corrective_candidate
```

The first independent exact-candidate review stopped commit `b31c289` on 21
contract findings. Corrective candidate `299aa865` closed that ledger, but its
independent final freeze review returned `STOP_WITH_FINDINGS` on three residual
areas: successful harness-trace representation, exact liveness/asymmetric-
failure execution rows, and stored-byte/digest identity. Candidate `fd2d62f`
closes those findings with one detached JSON byte law, distinct logical/stored
binding orders, harness-only liveness observation plus the separate LV03 child
report, exact LV01–LV06 and AF01–AF04 schedules/outcomes, absorbing global-guard
failure, and successful/fault/liveness-complete harness traces. The precommit
residual audit returned `ACCEPT_FOR_CORRECTIVE_COMMIT` after 20/20 validator
checks, 32/32 declared mutations, 8/8 additional targeted mutations, 215/215
regressions, and 109/109 relative links.

The candidate changes only five governing/specification documents and the
non-release review validator. It changes no Phase-4 runtime Python, Unreal
source, evidence, artifact, manifest, capacity record, or sealed predecessor
byte. Review commit `fd2d62f404936eb961d7d1571204f1a352439bd7` and tree
`b8b7bd6259d72c685ac5dee3c1488b19025b0fb3`, not this later excluded handover
commit. No implementation, freeze, evidence, release, capacity advancement,
Phase 5, or push follows from this record.

## Phase-4 specification-review opening — 2026-08-29

This is the current operational record. It supersedes the successor-selection,
Phase-4, current-working-unit, and next-gate statements in every later
historical section of this handover.

```yaml
selection_commit: b31c2895aec82c9688f6525219598b9ac1a274cc
selection_tree: 5bc3b0701973731d67a27ab7700f9b2935f96554
selection_subject: Open cross-domain occupancy materialization review
governing_continuation: 0.7.0-draft.79
working_unit: Cross-Domain Canonical Occupancy Materialization Proof v0.1.0-draft.0
phase: 4
candidate_proof_harness_identity: CrossDomainCanonicalOccupancyMaterializationProof.v1
candidate_simulation_identity: 0.7.0-draft.79
specification_status: specification_review_only
freeze_status: not_frozen
independent_review_status: pending
implementation_authority: none
unreal_source_change_authority: none
evidence_authority: none
release_authority: none
capacity_record: THE_CITY Development Capacity and Progress Note v0.1.11
capacity_advancement: none
phase_5: closed
push_performed: false
next_gate: independent_specification_review_of_exact_selection_commit
```

The candidate asks whether the exact sealed Phase-2 R0 → Rtransit → Rfinal
occupancy chain can be represented across the same two original,
simultaneously live Phase-3 Unreal domains while canonical records retain sole
occupancy, transition, completion, reservation, ledger, ancestry, and successor
authority. The proposed exhaustive endpoint representation is:

```text
                 domain A    domain B    total subject Actors
R0                  0           1                  1
Rtransit            0           0                  0
Rfinal              1           0                  1
```

The Rtransit zero is not accepted as bare absence. Each domain must publish one
positive exact-head anchor, and a separate live-world probe must independently
census zero subject Actors. The draft also binds two canonical commits, two
stale/refresh cycles in the same original A/B processes, all four refresh-order
combinations, immediate-successor-only refresh, atomic anchor/Actor
publication, asymmetric failures at both cycles, canonical-independence
controls, field-bound identity and authority adversaries, source/dataflow
closure, and a future exact release contract.

The selection commit changes only the new proof draft and four current
governing documents. It changes no Python, Unreal, capacity, Phase-3 artifact,
or Phase-3 manifest byte. The three Phase-2 canonical artifacts match their
sealed raw SHA-256 identities. An isolated export of Phase-3 seal commit
`f72d6fbb87bcc5a047db0ab12f7447614ebee1fc` verifies 111/111 release members
with all 41 verifier adversaries rejected. No independent Phase-4 verdict has
yet been rendered. Review exact selection commit
`b31c2895aec82c9688f6525219598b9ac1a274cc`; do not review this later excluded
handover commit as though it were the candidate.

## Phase-3 forward seal — 2026-08-29

This remains the authoritative Phase-3 seal record. Its successor-selection,
Phase-4, and next-gate fields describe the moment of sealing and are superseded
by the current section above.

```yaml
sealed_candidate_commit: 4e14b39a01ba712bfe559d004b0383fc7d9db7d6
sealed_candidate_tree: 9cd3d8568959cb6b3cfd5e9f06383a7efea6dd78
seal_commit: f72d6fbb87bcc5a047db0ab12f7447614ebee1fc
seal_tree: 5916ade67e25bf004d12df74b980fde4ec39bfaa
seal_subject: Seal simultaneous physical domains proof
seal_form: forward_history_preserving
independent_verdict: ACCEPT_FOR_OPERATOR_SEAL_REVIEW
independent_findings: none
artifact_roles: 44/44
release_members_excluding_manifest: 111/111
release_verifier_adversaries: 41/41_rejected
evidence_status: passed_and_sealed
capacity_record: THE_CITY Development Capacity and Progress Note v0.1.11
capacity_advancement: none
successor_selected: false
phase_4: closed
push_performed: false
next_gate: separate_operator_successor_selection_and_freeze_decision
```

The forward seal changes only six governing records and the self-excluding
manifest. The accepted Phase-3 Python and Unreal implementation, all 44
artifacts, and Development Capacity v0.1.11 remain byte-identical to candidate
`4e14b39a01ba712bfe559d004b0383fc7d9db7d6`. Candidate verification from an
isolated export passed 12/12 specification checks and 111/111 release members;
the seal tree passed 111/111 release verification and 215/215 predecessor
regressions. The verifier's frozen acquisition-time `evidence remains
unsealed` output does not self-authorize or negate the later governing seal.

This handover remains excluded from the 111-member release. The exact sealed
release is commit `f72d6fbb87bcc5a047db0ab12f7447614ebee1fc`, tree
`5916ade67e25bf004d12df74b980fde4ec39bfaa`. No push occurred. No successor or
Phase-4 work was authorized by that seal itself.

## Current corrected candidate — 2026-08-29

This section supersedes every later candidate identity, count, verdict, and
review command in this historical handover. The older sections remain only as
the record of prior review rounds.

```yaml
repository_realpath: /Users/boandersson/Desktop/Games/THE_CITY
branch: main
reviewed_candidate_commit: 4e14b39a01ba712bfe559d004b0383fc7d9db7d6
reviewed_candidate_tree: 9cd3d8568959cb6b3cfd5e9f06383a7efea6dd78
reviewed_candidate_subject: Correct Phase 3 focused test evidence count
independent_verdict: ACCEPT_FOR_OPERATOR_SEAL_REVIEW
independent_findings: none
review_testimony_is_a_seal: false
evidence_status: unsealed
capacity_advancement: none
phase_4: closed
push_performed: false
next_gate: operator_evidence_and_seal_review
```

The reviewed release includes the implementation correction at
`990b6233af13aa27ac10a404de1355e22aa332d9`, the complete source-input closure
at `3e72568c29ee2878e2a4bce92056622fc191a102`, and the manifest-bound evidence
count correction at the reviewed candidate above. The independent reviewer
confirmed the exact final archive, found no remaining issue, and reported:

```yaml
specification_validator: 12/12
specification_adversaries: 35/35_rejected
focused_tests: 41/41
predecessor_regressions: 215/215
source_checks: 41/41
source_adversaries: 16/16_rejected
classified_input_api_occurrences: 71
lstat_occurrences: 2
cpp_call_surface_occurrences: 2465
exact_cpp_source_identities: 5/5
artifact_roles: 44/44
release_members: 111/111
release_verifier_adversaries: 41/41_rejected
manifest_self_excluding: true
```

The fresh rebuilt-Unreal evidence population contains 177 process executions:
154 accepted unique process closures, 22 one-field binding rejections, and one
coordinated witness/launch relabel rejection. The accepted evidence reconciles
170 embeddings to those 154 identities, with 2,178 runtime-trace events and 464
stdin events. It retains exact three-stage successful physical traces and the
exact 24 physical-fault prefixes. The accepted UE identity is
`5.8.0-55116800+++UE5+Release-5.8`.

The source audit now accounts for both live `lstat` calls and is backed by an
exact call-surface census plus exact translation-unit byte identities. It
rejects the prior reachable `FFileHelper` branch, an added `lstat`, unclassified
`readlink`, `access`, and `getpid` calls, and a direct `environ` read that adds no
call token. The binding witness is derived from child-visible process-root
state, and the coordinated relabel is rejected before provenance or
materialization with zero trace events. The verifier also rejects deletion of
the three successful refresh physical-read events after trace renumbering and
summary recomputation.

This handover is excluded from the 111-member release. Its later documentation
commit is therefore not the independently reviewed release tree. Review or seal
only commit `4e14b39a01ba712bfe559d004b0383fc7d9db7d6` and tree
`9cd3d8568959cb6b3cfd5e9f06383a7efea6dd78`. No push, seal, capacity change, or
Phase-4 work follows from this record.

## 1. Read this first

The current work is Phase 3, **Simultaneous Physical Domains Proof v0.1.1**.
Its specification is frozen and its bounded implementation/evidence candidate is
complete. Its evidence is **not sealed**. The latest independent review accepted
the final verifier correction and opened passage to a separate evidence/seal
review at one exact commit. It did not seal the proof, advance capacity, open
Phase 4, or authorize a push.

Do not conflate these three statements:

1. the frozen Phase-3 specification authorized bounded implementation;
2. the bounded implementation and evidence candidate have been produced; and
3. the evidence has not yet received a separate seal.

The next working unit is therefore **independent evidence/seal review of the
exact candidate**, not more implementation.

## 2. Exact repository identity

The accepted verifier-correction candidate is:

```yaml
repository_realpath: /Users/boandersson/Desktop/Games/THE_CITY
branch: main
candidate_commit: 06f058311ca00cab0b98ca376d327ca0a26a9a5e
candidate_tree: 0921444726984314194e318abf51b9bd8cc7293e
candidate_subject: Bind Phase 3 verifier evidence rows
local_tracking_ref_observed: origin/main
local_origin_main_before_handover_commit: f19576457bc9f2f838d6a94a70258434829ebe4e
candidate_ahead_behind_before_handover_commit: 0_behind_3_ahead
remote_branch_containing_candidate_before_handover_commit: none_observed
fetch_during_handover: false
push_during_handover: false
candidate_worktree_before_handover_creation: clean
legacy_handover_retained: THE_CITY Current Proof State and Repo-Agent Instruction - v0.1.0.md
handover_relationship: later_documentation_only_descendant_of_candidate
```

The `origin/main` statement is about the existing local remote-tracking ref. No
network fetch was performed to prove server state.

This handover is intentionally outside the exact candidate commit and is
committed later as its informational successor. The legacy current-state
handover remains byte-identical because it is a hash-bound release member.
Review the candidate by full commit and tree identity, preferably from an
isolated `git archive`; do not treat `handover.md` or its later commit as part
of the candidate's 111-member release.

## 3. Governing record

```yaml
governing_continuation: 0.7.0-draft.77
latest_capacity_record: THE_CITY Development Capacity and Progress Note v0.1.11
latest_sealed_proof:
  name: Canonical Occupancy Transition Proof
  version: 0.1.0
  seal_commit: 638e1accc2076814b1f05458b2a55ecaa0a16232
active_proof:
  name: Simultaneous Physical Domains Proof
  version: 0.1.1
  proof_harness_identity: SimultaneousPhysicalDomainsProof.v1.1/0.7.0-draft.77
  specification_status: frozen
  implementation_status: exact_bounded_candidate_complete
  evidence_status: unsealed
  capacity_advancement: none
  next_gate: independent_evidence_and_seal_review
phase_4: closed
push_authority: not_granted_by_latest_review
```

Read governing material in this order:

1. `THE_CITY Current Proof State and Repo-Agent Instruction - v0.1.0.md`
2. `Simultaneous Physical Domains Proof - v0.1.1.md`
3. `Simultaneous Physical Domains Proof Evidence - v0.1.1.md`
4. `Simultaneous Physical Domains Proof - v0.1.1 SHA256SUMS.txt`
5. `THE_CITY Development Capacity and Progress Note - v0.1.11.md`
6. `THE_CITY Developer Snapshot - v0.1.0.md`
7. `Co-op Open-City FPS Simulation - v0.7 Working Continuation.md`

When summaries conflict, repository identity, the frozen specification, exact
evidence, manifest, verifier, latest capacity record, and current-state
instruction outrank older descriptive prose.

## 4. Phase-3 question and bounded answer candidate

The frozen question is:

> Can two process-isolated Unreal representation domains remain simultaneously
> alive against one canonical head, survive an independently committed
> canonical H0-to-H1 transition without participating in that transaction, and
> independently rebind to H1 while any domain still representing H0 is
> mechanically classified as stale and incapable of current-head canonical
> authority?

The candidate answer is bounded to one exact Phase-1 topology transition:

```yaml
canonical_payload_schema: CanonicalSpatialTopologyIdentityPayload.v1
canonical_source_simulation: 0.7.0-draft.61
canonical_sites:
  - topology_site_0001
  - topology_site_0002
canonical_route: topology_route_0001
R0_raw_sha256: 5e57c04875cfaead69f4cd6aaffeee2f788a2c1f5a820a56fb7083f8f7e861ed
H0_record_hash: 666d75281d3478e586edd12464d2736169f423c2d7b128bd3d2d2b1b2b826b29
H0_route_access: available
canonical_mutation: topology_route_0001.access_state_available_to_blocked
R1_raw_sha256: 7ac7ece5c142ac4dee83abc6e83f7845d85dfc7f055ca6d678b7f04bdf1d795a
H1_record_hash: 78cc5ffe0c4758c296d8fee0bc2a95e230be0bec0a4aab680806eb670500804a
H1_route_access: blocked
canonical_transition_branches_byte_identical: true
new_canonical_payloads: 0
new_canonical_fields: 0
new_canonical_mutations: 0
new_canonical_resolvers: 0
```

Domain projections are detached and noncanonical:

```yaml
domain_A:
  canonical_site_projection: topology_site_0001
  shared_route_projection: topology_route_0001
domain_B:
  canonical_site_projection: topology_site_0002
  shared_route_projection: topology_route_0001
site_scope_is_canonical_occupancy: false
representation_identity_is_canonical_identity: false
domain_local_state_is_authoritative: false
```

Phase 1 destroyed its source representation before H1. Phase 3 deliberately
does not inherit that lifecycle. The same original A and B Unreal process
instances must remain alive across the independent canonical H0-to-H1 commit
and then rebind independently to H1.

## 5. Authority separation

There is one canonical authority plane and one disposable physical protocol.

```text
sealed Phase-1 R0/H0 + exact H0-bound boundary
                    |
                    v
        sealed Phase-1 resolver/commit
                    |
                    v
             exact R1/H1 truth

harness-private observer + physical guard + UE domains
                    |
                    v
 disposable representation correspondence only
```

The canonical resolver owns canonical mutation, history, ledger, ancestry,
unresolved work, serialization, and hashing. It must not read or discover:

- the physical-current-head guard;
- `current_head_observation.json`;
- Unreal/domain state;
- projection, Actor, cache, collision, physics, or retained scalar state;
- process identity, liveness, receipt, refresh order, or fault selection.

The harness owns physical classification. Unreal reports represented-record
and local-publication facts only. Unreal never receives or emits the harness
classifications `synchronized`, `head_unconfirmed`, or `stale`.

H0 remains valid immutable history after H1. What dies at H1 is every H0-bound
claim to current-head authority.

## 6. Physical guard law

The exact guard states are:

```yaml
open_for_H0:
  accepted_physical_head: H0
  claim_acceptance: H0_only
  refresh_eligibility: false
closed_for_H0_to_H1:
  accepted_physical_head: none
  claim_acceptance: false
  refresh_eligibility: false
open_for_H1:
  accepted_physical_head: H1
  claim_acceptance: H1_only
  refresh_eligibility: stale_H0_to_exact_H1_only
failed_closed:
  accepted_physical_head: none
  claim_acceptance: false
  refresh_eligibility: false
  reopening_in_this_proof: prohibited
```

The guard governs only physical representation-claim acceptance, receipt and
live-observation acceptance, and refresh eligibility. It cannot gate canonical
execution.

Normal reopening is legal only after exact H1 observation has atomically
published, been reread and reverified, and every affected H0 domain has been
durably classified stale. A failure before that completion ends the witness
`failed_closed`.

The guard-open control must still commit byte-identical R1/H1. It then fails
only the physical protocol: guard `failed_closed`, both domains terminal
`protocol_invalid(H0/H1)`, no refresh, and no rollback or rewrite of H1.

## 7. Physical head-state machine

The admitted states and permissions are:

| State | Guard | Current-head representation claim | Refresh | Local execution |
| --- | --- | --- | --- | --- |
| `synchronized(H0)` | `open_for_H0` | disposable correspondence only | no | nonconsequential |
| `head_unconfirmed(H0)` | `closed_for_H0_to_H1` or `failed_closed` | no | no | quarantined nonconsequential |
| `stale(H0/H1)` | `open_for_H1` | no | exact H1 once | quarantined nonconsequential |
| `synchronized(H1)` | `open_for_H1` | disposable correspondence only | no | nonconsequential |
| `invalid` | recorded matching state | no | no | diagnostics/termination only |
| `protocol_invalid(H0/H1)` | `failed_closed` | no | no | diagnostics/termination only |

All states prohibit canonical evidence, scheduling, mutation, truth
publication, strategic authority, and head selection.

Exact normal transition:

```text
unbound
  -> synchronized(H0)
  -> head_unconfirmed(accepted H0)
  -> stale(accepted H0, current H1)
  -> synchronized(H1)
```

Exceptional transitions:

```text
pre-publication refresh rejection -> remain stale(H0/H1)
partial/unprovable local publication -> invalid, halted
H1 commit while guard stayed open_for_H0 -> protocol_invalid, terminal
head observation/publication failure -> head_unconfirmed + failed_closed
invalid or protocol_invalid -> no proof-local recovery transition
```

A stale H0 representation may execute only bounded, local,
nonconsequential work. It cannot emit current-head evidence, schedule or mutate
canonical work, label H0 facts current, publish an H1 acceptance receipt,
repair H1, influence another domain, or preserve H0 as a competing city.

## 8. Refresh and retained-state law

Every authoritative-derived H1 representation fact is reconstructed solely
from exact H1 plus the exact role/H1 detached projection. The only stale local
state allowed to cross refresh is:

```yaml
retained_local_state:
  - nonconsequential_tick_counter
  - cosmetic_phase_token
  - diagnostic_counter
```

Stale Actor, cache, collision, physics, receipt, capability, and H0-derived
representation state must not merge into or select H1 truth. A refresh remains
private until one complete local publication point.

The normal refresh invocation is one exact canonical JSON line on the original
stdin pipe with one exact role-specific, read-only, three-file bundle. Alternate
watchers, pollers, signals, FIFOs, sockets, shared directories, pipes, or a
second refresh invocation are prohibited.

The operational head observation and every value derived from it remain
harness-private. No head-observation path, byte, digest, validity flag, or
classification may reach Unreal.

## 9. Live evidence envelope

The candidate records 11 frozen live-process witnesses:

```yaml
W1: A_then_B_refresh_with_uninterrupted_liveness
W2: B_then_A_refresh_with_uninterrupted_liveness
W3: one_exact_compiled_nonconsequential_counter_step_per_stale_original_process
W4: post_commit_pre_head_publication_failure_fails_closed
W5_baseline: retained_state_baseline
W5_perturbed: poison_then_rederive_H1_with_only_three_scalars_retained
W6_A: A_synchronized_B_refresh_fails_and_remains_stale
W6_B: B_synchronized_A_refresh_fails_and_remains_stale
W7_A: destroy_A_without_changing_H1_or_B
W7_B: destroy_B_without_changing_H1_or_A
W8: guard_open_exact_canonical_commit_then_terminal_protocol_invalid
```

The independent live-UE probe observes the published physical route surface,
not the receipt or authoritative-derived JSON:

```yaml
domain_A_H0: available
domain_B_H0: available
domain_A_H1: blocked
domain_B_H1: blocked
same_original_process_binding_before_after: true
receipt_independent_probe: true
player_pawns_or_possessions_observed: 0
phase_3_actor_input_paths_observed: 0
```

W3 is valid only because each original stale UE process receives one named
`increment_nonconsequential_tick_counter_once` command and reports an exact
`7 -> 8` local counter change while represented H0, Actor identity, and all
authority surfaces remain unchanged. CPU delta is supplemental and cannot
satisfy W3.

The executable negative surfaces are:

```yaml
head_observation_fault_points: 9/9_fail_closed
illegal_guard_transitions: 8/8_rejected
refresh_fault_rows: 36/36_fresh_process_bound_real_adapter_or_router_boundaries
physical_observation_fault_rows: 24/24_fresh_process_bound_live_probe_router_or_harness_boundaries
current_head_authority_cases: 37/37_exact_case_ID_to_action_rows
canonical_measurement_per_applicable_case: record_ledger_ancestry_schedule
canonical_R1_rollback_or_rewrite: none
partial_current_head_authority: none
```

Every applicable adversary carries independently serialized before/after
canonical record, ledger, ancestry, future schedule, and mutation-count
measurements. A hard-coded `canonical_H1_unchanged` boolean is not accepted.
Case 19 is the sole authority adversary whose lawful canonical relation is an
exact H0-to-H1 commit; it must still end the physical protocol failed closed.

## 10. Proof-semantic input closure

The audit binds:

- ordered argv;
- hashed environment entries;
- launch cwd;
- inherited descriptors 0/1/2;
- executable, project, module, engine, and loaded-system-image identities;
- exact three-file launch and refresh bundles;
- original stdin command sequence;
- exact W3 local-step command where applicable; and
- exact fault-arm command only in the corresponding fault witness.

It prohibits any undeclared input capable of changing Phase-3 semantics,
including another-domain state, expected result, head observation, guard,
project proof records, alternate file opens, dynamic selectors, or player/input
paths.

UE `-game` may create one inert engine-base player controller, but Phase 3 owns
no Pawn, possession, player input component, auto-receive binding, or reachable
player/input path. Both Pawn classes are null and the Phase-3 dispatch bypasses
the legacy controller/Pawn route.

## 11. Frozen implementation surface

Only these four Phase-3 Python paths are implementation members:

```text
proof_kernel/simultaneous_physical_domains.py
proof_kernel/simultaneous_physical_domains_harness.py
proof_kernel/test_simultaneous_physical_domains.py
proof_kernel/verify_simultaneous_physical_domains_release.py
```

Only these eight new Unreal paths are implementation members:

```text
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainCommandRouter.cpp
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainCommandRouter.h
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainProofAdapter.cpp
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainProofAdapter.h
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainRepresentationActor.cpp
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalDomainRepresentationActor.h
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalRebindProbe.cpp
CityMaterializationProof/Source/CityMaterializationProof/SimultaneousPhysicalRebindProbe.h
```

The sole bounded existing-source change is the Phase-3 dispatch branch in:

```text
CityMaterializationProof/Source/CityMaterializationProof/CityProofGameMode.cpp
```

Other authorized record surfaces are:

```yaml
evidence_document: Simultaneous Physical Domains Proof Evidence - v0.1.1.md
artifact_directory: proof_kernel/SimultaneousPhysicalDomainsProofRecords/
artifact_files: exactly_44_regular_non_symlink_members_and_no_others
release_manifest: Simultaneous Physical Domains Proof - v0.1.1 SHA256SUMS.txt
release_members_excluding_manifest: exactly_111
manifest_self_excluding: true
```

`proof_kernel/validate_simultaneous_physical_domains_spec.py` is review-time QA
only. It is outside the runtime, 44-artifact directory, 111-member manifest,
and implementation authority.

All other existing source, project, and config paths are hash-bound
dependencies. Do not expand this surface without returning to specification
review.

## 12. Final verifier correction

Commit `06f058311ca00cab0b98ca376d327ca0a26a9a5e` changes exactly four paths:

```text
M  Simultaneous Physical Domains Proof - v0.1.1 SHA256SUMS.txt
M  Simultaneous Physical Domains Proof Evidence - v0.1.1.md
M  proof_kernel/SimultaneousPhysicalDomainsProofRecords/simultaneous_physical_domains_source_audit.json
M  proof_kernel/verify_simultaneous_physical_domains_release.py
```

Its diff is 975 insertions and 45 deletions. It does not change the
specification, Unreal source, harness, core proof logic, focused test file,
capacity, or live witness artifacts.

The correction now enforces:

- exact fault-matrix and row schemas;
- row surface, stage, edge, head, and derived run-ID binding to command, arm
  receipt, boundary result, owner, target, and peer histories;
- structural peer evidence and liveness bound to the exact peer process;
- unique and disjoint target/peer process IDs, binding digests, birth tuples,
  bound results, and domain-evidence digests across each matrix;
- an independent 37-row authority expectation table for execution path,
  rejection stage, and reason;
- exact authority concrete inputs and cross-references to live witness/fault
  artifacts; and
- 33 isolated verifier adversaries, including the previously accepted reused
  row, relabelled row, dead peer, swapped authority evidence, arbitrary
  nonempty authority fields, CPU-only W3, Python-only live matrices, and
  hard-coded unchanged-history forms.

Exact relevant hashes at the candidate tree:

```yaml
release_verifier_sha256: 1bc7f5c593f5ab70bb3bc2ca471fbd3812705930600c3a46e251f32e82b25c76
release_manifest_sha256: 27e6ca49800bcf99cd327b877743b9044e46616b05bb7427bfb15f0a09ae959b
evidence_document_sha256: 41c93b0d6413de542e38bedac7041b36ad43b1ec98905f39430fe6231bdcdbb0
source_audit_artifact_sha256: 52f997e6485748415d8c0eeecc7979bb168b0bd8d840c2c27a3f5dc0552cb77b
```

The lack of a UE rebuild or live witness reacquisition for this last commit is
intentional: no runtime, harness, specification, core, test, or live-witness
input changed. The source-audit artifact, evidence summary, and manifest were
regenerated because they hash-bind the verifier.

## 13. Corrective lineage

Preserve history. Do not rewrite or collapse this sequence:

```yaml
- commit: f19576457bc9f2f838d6a94a70258434829ebe4e
  tree: 80168e526b122fdcc6726d8808bd3d4ad76f3994
  subject: Implement simultaneous physical domains proof
  state: pushed_origin_main_but_later_STOP_reviewed
- commit: c8aeaca3b020c1ca17172464cbdb68437eb099cf
  tree: a4f15bff8b55161f35bcc909a1b5d36f4e1921f3
  subject: Correct simultaneous physical domains evidence
  state: forward_correction_but_later_STOP_reviewed
- commit: e33c8938b498b1b4bd2ae78c6e873d616e344ba0
  tree: 040253b7e7b8199aa40b0b8824fc84f588a64519
  subject: Correct Phase 3 executable evidence
  state: material_correction_accepted_in_design_but_verifier_gap_found
- commit: 06f058311ca00cab0b98ca376d327ca0a26a9a5e
  tree: 0921444726984314194e318abf51b9bd8cc7293e
  subject: Bind Phase 3 verifier evidence rows
  state: verifier_correction_independently_accepted_candidate_for_seal_review
```

Review the full `06f0583...` tree. The four-file scope describes only the last
correction, not the cumulative implementation delta from `origin/main`.

## 14. Verification record

The candidate and latest independent verifier-correction review report:

```yaml
specification_validator: 12/12
specification_adversaries: 35/35_rejected
focused_phase_3_tests: 33/33
predecessor_regressions: 215/215
UE_version: 5.8.0-55116800-++UE5+Release-5.8
UE_editor_build: passed_with_DisableUnity_at_runtime_evidence_candidate
live_witnesses: 11/11
artifact_roles: 44/44
release_members: 111/111
release_verifier_adversaries: 33/33_rejected
manifest_self_excluding: true
canonical_replay: byte_identical
W1_W2_semantic_replay: equal
diff_checks: clean
candidate_commit_worktree_at_review: clean
```

The conversation-provided independent review classified the final correction
`PROGRESS_RISK — CONTINUE`, confirmed the exact commit/tree/four-file scope and
both reported correction hashes, replayed the previously accepted mutations,
and found no critical issue in that correction. This is review testimony, not a
repository seal record.

## 15. Exact read-only review commands

Begin every new Desktop-attached session from `/Users/boandersson/Desktop`:

```sh
"./Tools & Systems/Desktop Organizer/desktop-frontdoor" run-system
```

Then bind the repository snapshot:

```sh
cd "/Users/boandersson/Desktop/Games/THE_CITY"
git status --porcelain=v2 --branch
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
git rev-list --left-right --count origin/main...HEAD
git branch -r --contains HEAD
```

Because this handover is outside the candidate commit, use an isolated export
for exact evidence/seal review:

```sh
phase3_review_root="$(mktemp -d /private/tmp/thecity-phase3-review.XXXXXX)"
trap 'rm -rf -- "$phase3_review_root"' EXIT
git archive 06f058311ca00cab0b98ca376d327ca0a26a9a5e | tar -x -C "$phase3_review_root"
cd "$phase3_review_root"
test "$(git -C /Users/boandersson/Desktop/Games/THE_CITY rev-parse '06f058311ca00cab0b98ca376d327ca0a26a9a5e^{tree}')" = \
  "0921444726984314194e318abf51b9bd8cc7293e"
```

Validate the frozen document and its in-memory adversaries:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 proof_kernel/validate_simultaneous_physical_domains_spec.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 proof_kernel/validate_simultaneous_physical_domains_spec.py --self-test
```

Verify artifacts and the complete release without writing the release:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 proof_kernel/verify_simultaneous_physical_domains_release.py artifacts
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 proof_kernel/verify_simultaneous_physical_domains_release.py verify
```

Do **not** run the verifier's `write-release` mode during read-only review.

Run the focused suite:

```sh
cd "$phase3_review_root/proof_kernel"
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 -m unittest -v test_simultaneous_physical_domains.py
```

Run the complete predecessor regression suite:

```sh
cd "$phase3_review_root/proof_kernel"
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/private/tmp/thecity_pycache \
  python3 -m unittest -v \
  test_kernel.py test_roundtrip.py test_contention.py \
  test_unreal_authority_boundary.py test_deployment_opportunity.py \
  test_live_commitment.py test_shared_state_interference.py \
  test_bounded_agent_selection.py test_resolution_semantics_substrate.py \
  test_causal_lod_equivalence.py \
  test_record_relative_chronological_resolution.py \
  test_external_input_boundary.py test_same_clock_successor_semantics.py \
  test_integrated_unreal_promotion_unload_repromotion.py \
  test_concurrent_external_evidence_arbitration.py \
  test_canonical_spatial_topology_identity.py \
  test_canonical_occupancy_transition.py
```

Check committed diffs from the live repository without changing it:

```sh
cd "/Users/boandersson/Desktop/Games/THE_CITY"
git diff --check 06f058311ca00cab0b98ca376d327ca0a26a9a5e^ \
  06f058311ca00cab0b98ca376d327ca0a26a9a5e
git diff --check origin/main...06f058311ca00cab0b98ca376d327ca0a26a9a5e
```

Expected bounded results are 12/12, 35/35 rejected, 44/44, 111/111,
33/33 verifier adversaries rejected, 33/33 focused tests, and 215/215
regressions.

The recorded UE build command is provenance, not a required rerun after the
QA-only verifier correction:

```sh
'/Users/Shared/Epic Games/UE_5.8/Engine/Build/BatchFiles/Mac/Build.sh' \
  CityMaterializationProofEditor Mac Development \
  '/Users/boandersson/Desktop/Games/THE_CITY/CityMaterializationProof/CityMaterializationProof.uproject' \
  -WaitMutex -DisableUnity
```

## 16. Seal-review acceptance boundary

The next reviewer must evaluate the full candidate, not merely report that its
commands print PASS. At minimum, bind findings to the exact commit/tree and
cover:

1. canonical authority ownership and physical/canonical separation;
2. architectural fit and absence of a second authority path;
3. failure atomicity, stale quarantine, illegal transitions, peer liveness,
   partial publication, and terminal states;
4. Git identity, artifact identities, manifest identity, and stable snapshot;
5. positive, negative, deterministic, replay, and actual live-path evidence;
6. surviving provenance, source/dataflow audit, and non-self-validating oracles;
7. exact scope and all non-claims; and
8. proof-local macOS/UE operational cost without promoting it to production
   architecture.

An evidence/seal review should explicitly decide whether the exact 44 artifacts
and self-excluding 111-member release prove the frozen acceptance statement.
It must not infer capacity or successor authority from a passing verifier.

If that separate review accepts the full exact candidate, stop and obtain the
operator's exact forward-seal instruction. Do not invent seal mechanics,
rewrite history, advance capacity, open Phase 4, or push automatically.

## 17. Known informational corpus residue

Two manifest-bound descriptive surfaces still carry older summary text:

- `README.md` names continuation `0.7.0-draft.76`, proof identity
  `SimultaneousPhysicalDomainsProof.v1 / 0.7.0-draft.72`, validator counts
  11/11 and 30, and a 110-member release.
- the continuation file header still says `0.7.0-draft.76`, and its late
  `Next working unit` paragraph still calls the active proof v0.1.0 with a
  44/110 candidate, although the same file contains the later draft.77 entry
  and current v0.1.1/44/111 record.

The current-state instruction, developer snapshot, v0.1.1 specification,
v0.1.1 evidence, validator, verifier, and manifest consistently identify
draft.77, v1.1, 12/12, 35/35, and 111 members. The latest independent
verifier-correction review accepted the exact tree containing the older prose
and reported no critical finding.

Do not silently edit either manifest-bound file during review. Their final
disposition belongs in the evidence/seal decision. If correction is required,
make a forward candidate, update every dependent source-audit/evidence/manifest
binding, rerun all gates, and seek independent review again. Never rewrite the
pushed or corrective history.

## 18. Explicit non-claims and closed work

The candidate does not prove or authorize:

- canonical occupancy materialization in Phase 3;
- Q, BQ, QA, QB, BEXT, admission, arbitration, or live external input;
- physical movement, navigation, interpolation, distance, speed, travel time,
  arrival, route progress, route occupancy, traffic, or congestion;
- player embodiment, split players, two-player gameplay, or 1-to-4-player
  topology;
- networking, replication, rollback, reconciliation, retry, reconnect, host
  migration, or save/load;
- World Partition, streaming, streaming bubbles, production materialization,
  or city-scale performance;
- arbitrary domain counts, dynamic lifecycle management, or cross-domain
  causal propagation;
- new canonical topology, scheduler, resolver, payload, or movement law;
- stochastic identity, generalized planning, production architecture, Phase 4,
  Phase 5, or Phase 6; or
- any capacity advancement beyond v0.1.11.

## 19. Next-session decision table

```yaml
if_candidate_commit_or_tree_object_differs_from_recorded_identity:
  action: stop_and_reconcile_identity_before_using_candidate_claims
if_HEAD_is_the_documentation_only_handover_successor:
  action: retain_legacy_member_and_review_exact_candidate_from_git_archive
if_HEAD_contains_other_changes_after_candidate:
  action: classify_each_change_and_do_not_attribute_it_to_the_candidate
if_reviewed_paths_change_during_review:
  action: invalidate_affected_findings_and_return_WAIT
if_any_verifier_or_test_gate_fails:
  action: do_not_seal; preserve_history; report_exact_failure
if_full_evidence_review_requests_correction:
  action: require_operator_authority; correct_forward; regenerate_bindings; rereview
if_full_evidence_review_accepts_exact_candidate:
  action: request_exact_operator_forward_seal_instruction
if_user_requests_push_before_seal_decision:
  action: clarify_that_push_does_not_seal_and_report_exact_target
if_seal_is_later_completed:
  action: keep_capacity_v0.1.11_and_Phase_4_closed_until_separate_decision
```

## 20. Short return card

```text
The exact Phase-3 candidate remains commit
06f058311ca00cab0b98ca376d327ca0a26a9a5e, tree
0921444726984314194e318abf51b9bd8cc7293e. A later documentation-only commit
adds this handover while retaining the hash-bound legacy handover. Phase 3
v0.1.1 is frozen, implemented, and evidenced as an exact
44-artifact/111-member candidate. The final verifier correction has independent
CONTINUE review. Evidence itself is still unsealed. Capacity remains v0.1.11;
Phase 4 and push remain closed. The next work is an independent full
evidence/seal review of the exact candidate commit.
```

The record is ready for that review. It is not yet a seal.
